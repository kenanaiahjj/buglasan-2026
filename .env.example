# Leave empty while the prototype uses the in-memory demo client.
VITE_VOTING_API_URL=

# Optional canonical route for returning from hosted checkout.
# Leave empty to return supporters to the page where they started.
VITE_VOTING_RETURN_URL=
